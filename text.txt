# Разработали 3 приложения
# accounts
# orders
# products
# ==============================
# Задавать вопросы касательно того что мы разработали ?

# ORM
# Таблица --- на основе ООП --- описываете как CLASS --- МОДЕЛЬ
# Столбец --- поле
# Строка --- строка ---
# Данные которые хранятся в строке --- 1 объект на основе модели --- объекты
# Когда мы даже пытаемся вытащить данные из БД они вернутся нам в виде Python объектов ?
# ORM --- Наш написанный запрос на языке Python --- на язык SQL ---
# Нам придет результат в виде Python объектов
# Почему объект ?
# Потому ты создаешь таблицы в виде класса --- объект --- потому что это у нас ООП так работает
# Изначально класс - чертеж который, описывает будущий объект который будет создан на основе этого чертежа
# ==============================================================================================================
# Проект
# 1. Дизайн - по дизайну нашего сайта мы определяем на какие приложения нам нужно разделить функционал нашего будущего
# БЕК ЭНД
# 2. В каком приложении будут какие модели ?
# 3. В какой модели какие поля будут хранить наши данные ?
# ==============================================================================================================
# Фронт ЭНД --- приложение
# Пользователь видит --- созданный фронт энд
# Пользователь --- кликает например на фронтенде кнопки
# Каждое действие на веб сайте оценивается как веб запрос ---
# GET ---
# POST ---
# На фронтенде пользователь кликнул на карточку товара чтобы увидеть детальное описание этого товара !
# Фронтенда приложение должен показать страницу где будут данные этого товара
# Откуда в странице появляются эти данные ?

# Как общаются бек энд с фронтендом ? С помощью API.
# Бек энд разработчик изначально зная что и какие данные потребует приложение фронт энда
# Разрабатывает API ?
# API --- ссылки которые выдают данные из БД.

# Фронтенд разработчик настраивает свое приложение так чтобы оно отправляла запрос через ссылку API
# на бек энд, и получила данные в формате JSON и показала у себя эти данные в красивом в виде подставляя
# На нужные элементы в странице.
# API <--- GET чтобы получить информацию про ПРОДУКТ.
# ===========================
# Вот весь процесс:
📱 Клиент отправляет запрос
🧠 Django получает его
🔍 ORM делает SQL-запрос в БД
# Запрос в БД чтобы получить информацию которая нужна.
🗄️ БД возвращает строки
🔄 Django превращает их в Python объекты

📦 DRF сериализует в JSON
📤 Отправляет клиенту


# 👉 Пользователь → отправляет запрос → Django → идёт в базу данных → получает данные → возвращает ответ